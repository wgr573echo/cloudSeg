import os
# path='./data/cuttifmask/'

# #获取该目录下所有文件，存入列表中
# fileList=os.listdir(path)

# n=0
# for i in fileList:
    
#     #设置旧文件名（就是路径+文件名）
#     oldname=path+ os.sep + fileList[n]   # os.sep添加系统分隔符
    
#     #设置新文件名
#     newname=oldname.split("+")[0]+"_"+oldname.split("+")[1]
    
#     os.rename(oldname,newname)   #用os模块中的rename方法对文件改名
    
#     n+=1
x = float(0.745)
y = int(0)

print(x==y)